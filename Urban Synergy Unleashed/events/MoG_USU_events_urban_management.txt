namespace = usu_secret_ev

USU_secret_ev.1 = {
	type = country_event		# Needs to be an event, not scripted effect, to be re-triggered from error-handler.
	hidden = yes
	
	trigger = {
		NOT = { is_country_type = decentralized }
	}
	
	immediate = {
		usu_manage_urbanization_modifiers = yes
	}
}