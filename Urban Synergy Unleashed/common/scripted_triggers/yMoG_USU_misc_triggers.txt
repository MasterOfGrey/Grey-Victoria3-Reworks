# scope = state
has_world_market_access_history = {
	NOT = { owner = { has_law_or_variant = law_type:law_isolationism } } # Not true for Western Guangdong, but oh well.
	OR = {
		owner = { 
			any_scope_treaty = {
				binds = prev
				any_scope_article = {
					has_type = transit_rights
				}
			}
		}
		market = {
			any_scope_country = {
				any_scope_state = {
					has_port = yes # non functional in building history
				}
			}
		}
	}
}
