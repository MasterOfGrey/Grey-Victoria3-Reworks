COUNTRIES = {
	every_country = {
		limit = { NOT = { is_country_type = decentralized } }
		add_modifier = {
			name = mog_urbanization_start	# Ensures that at game start the urbanization amount is ballpark ok - fine tuned by event later.
		}									# We need this here for starting employment reasons
	}
}