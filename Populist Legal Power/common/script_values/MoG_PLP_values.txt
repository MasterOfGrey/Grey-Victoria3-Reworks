### Building level history increment (added because 
REPLACE_OR_CREATE:mog_ga_building_history_increment = {
	value = 1
	b:building_government_administration ?= {
		add = level
	}
}

# Called from country scope.

REPLACE_OR_CREATE:spa_mig_control_target_threshold = { value = 70 }	# Not used atm
